extend = "common.toml"

[env]
# Project information
TITLE = "RPGBE"
TITLEID = "RPGBE0001"

[config]
load_script = [
    """
    if [ ! -f common.toml ]; then
        curl -SsL https://github.com/rust-on-vita/common/raw/master/common.toml > common.toml
    fi
    """
]
